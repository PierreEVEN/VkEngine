name: C/C++ CI

on:
  push:
    branches: [ master ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2
    - name: configure
      run: ./configure
    - name: GenerateProjectFiles
      run: GenerateProjectFiles.sh
